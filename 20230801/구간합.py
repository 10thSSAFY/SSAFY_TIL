import sys
sys.stdin = open('4835.txt', 'r')

T = int(input())
for tc in range(1, T+1):
    N, M = map(int, input().split())
    Ai = list(map(int, input().split()))

    for st in range(???):
        # M개의 합을 구한다.
        sumV
        for idx in range(st, st+M):
            sumV +=
        if curMax < sumV:
            curMax = sumV
        if curMin > sumV:
            curMin = sumV
